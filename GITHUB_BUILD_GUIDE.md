# GitHub Actions 自动构建APK指南

## 🚀 什么是GitHub Actions？

GitHub Actions是GitHub的自动化工具，可以在云端自动构建APK，无需本地配置Android环境。

---

## ✨ 优势

- ✅ **无需本地环境**：不需要安装Android Studio、Flutter SDK
- ✅ **自动构建**：每次推送代码自动构建
- ✅ **云端构建**：使用GitHub强大的服务器
- ✅ **直接下载**：构建完成直接下载APK
- ✅ **版本管理**：所有构建历史记录保存

---

## 📋 使用方法

### 方式1：自动构建（推荐）

每次推送代码到 `main` 分支时，**自动触发构建**。

```bash
git add .
git commit -m "your message"
git push origin main
```

构建完成后，在GitHub仓库页面下载APK。

### 方式2：手动触发构建

1. **访问仓库**：https://github.com/dgr1771/aeryn-deutsch

2. **进入Actions页面**：
   - 点击顶部标签栏的 "Actions"

3. **选择工作流**：
   - 左侧选择 "Build APK (Quick)" - 快速构建
   - 或 "Build Android APK" - 完整构建

4. **点击 "Run workflow"**：
   - 选择分支：main
   - 点击绿色按钮 "Run workflow"

5. **等待构建完成**（约5-10分钟）

6. **下载APK**：
   - 构建完成后，进入该次构建记录
   - 滚动到底部 "Artifacts" 区域
   - 点击下载APK文件

---

## 📦 工作流说明

### Build Android APK（完整构建）

**触发条件**：
- 推送到main/master分支
- Pull Request
- 手动触发

**构建产物**：
- `app-release.apk` - 通用APK（推荐用于测试）
- `app-release.aab` - App Bundle（用于Google Play）
- 分架构APK（arm64-v8a, armeabi-v7a, x86_64）

**保留时间**：30天

### Build APK (Quick)（快速构建）

**触发条件**：仅手动触发

**构建产物**：
- `aeryn-deutsch-apk` - 通用APK

**保留时间**：90天

---

## 📥 下载APK步骤

### 步骤1：查看构建状态

1. 访问：https://github.com/dgr1771/aeryn-deutsch/actions

2. 查看最近的构建记录：
   - 🟢 绿色对勾 = 构建成功
   - 🔴 红色叉 = 构建失败
   - 🟡 黄色圆点 = 构建中

### 步骤2：下载APK

1. **点击构建记录**

2. **滚动到底部** "Artifacts" 区域

3. **点击下载**：
   - `app-release-apk` - 下载APK文件（.zip格式）
   - 解压后得到 `app-release.apk`

---

## 🔧 构建配置

当前配置：

- **Flutter版本**: 3.19.0 (Stable)
- **Java版本**: 17 (Zulu)
- **构建目标**:
  - android-arm (32位ARM设备)
  - android-arm64 (64位ARM设备)
  - android-x64 (64位x86设备)

修改构建配置：编辑 `.github/workflows/build-apk.yml`

---

## 📱 安装APK到手机

### 方法1：通过USB

```bash
# 启用USB调试后
adb install app-release.apk

# 如果需要覆盖安装
adb install -r app-release.apk
```

### 方法2：直接安装

1. 将APK文件传输到手机
2. 在文件管理器中点击APK文件
3. 允许安装来自未知来源的应用

---

## 🆘 常见问题

### 1. 构建失败怎么办？

**原因**：代码错误或依赖问题

**解决**：
- 查看构建日志（点击失败的构建记录）
- 检查错误信息
- 修复后重新推送

### 2. 找不到Artifacts？

**原因**：构建还在进行中

**解决**：
- 等待构建完成（黄色圆点变绿色）
- 刷新页面

### 3. APK安装失败？

**原因**：
- Android版本过低（需要Android 5.0+）
- APK签名问题

**解决**：
- 确认Android版本 >= 5.0
- 先卸载旧版本再安装

### 4. 构建太慢？

**原因**：首次构建需要缓存

**解决**：
- 后续构建会更快（3-5分钟）
- 使用 "Build APK (Quick)" 工作流

---

## 📊 构建历史

查看所有构建历史：

1. 访问：https://github.com/dgr1771/aeryn-deutsch/actions
2. 点击左侧 "Build Android APK" 或 "Build APK (Quick)"
3. 查看所有历史构建记录

每个构建记录包含：
- 提交信息
- 构建日志
- 构建产物（APK）
- 构建时间

---

## 🎯 最佳实践

### 开发阶段
- 使用自动构建：每次推送自动构建
- 保留最近的APK用于测试

### 发布阶段
- 创建Git标签：`git tag v1.0.0`
- 推送标签：`git push origin v1.0.0`
- 自动创建Release并附加APK

### 示例：创建发布版本

```bash
# 创建标签
git tag v1.0.0

# 推送标签到GitHub
git push origin v1.0.0

# GitHub Actions自动构建并创建Release
# 下载地址：https://github.com/dgr1771/aeryn-deutsch/releases
```

---

## 🔗 快速链接

- **仓库主页**: https://github.com/dgr1771/aeryn-deutsch
- **Actions页面**: https://github.com/dgr1771/aeryn-deutsch/actions
- **Release页面**: https://github.com/dgr1771/aeryn-deutsch/releases

---

**版本**: 1.0.0
**更新日期**: 2026-02-09
**制作**: Aeryn-Deutsch Team
