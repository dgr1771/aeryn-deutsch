name: Build Android APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout代码
        uses: actions/checkout@v4

      - name: 设置Java环境
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: 设置Flutter环境
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.19.0'
          channel: 'stable'
          cache: false

      - name: 清理并获取依赖
        run: |
          echo "=== 清理缓存 ==="
          flutter clean
          rm -rf build/
          echo "✅ 清理完成"
          echo ""
          echo "=== 获取依赖 ==="
          flutter pub get
          echo "✅ 依赖获取完成"

      - name: 验证V2 Embedding配置
        run: |
          echo "=== MainActivity.kt (V2 Embedding) ==="
          cat android/app/src/main/kotlin/com/aeryn/deutsch/MainActivity.kt
          echo ""
          echo "=== AndroidManifest.xml (flutterEmbedding) ==="
          grep -A 2 "flutterEmbedding" android/app/src/main/AndroidManifest.xml

      - name: 构建APK (Release)
        run: |
          echo "开始构建APK..."
          flutter build apk --release --target-platform android-arm,android-arm64,android-x64

      - name: 上传APK
        uses: actions/upload-artifact@v4
        if: success()
        with:
          name: app-release-apk
          path: build/app/outputs/flutter-apk/app-release.apk
          retention-days: 30

      - name: 构建成功信息
        if: success()
        run: |
          echo "## 🎉 构建成功！" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **提交**: ${{ github.sha }}" >> $GITHUB_STEP_SUMMARY
          echo "- **分支**: ${{ github.ref_name }}" >> $GITHUB_STEP_SUMMARY
          echo "- **APK位置**: Artifacts → app-release-apk" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 下载步骤" >> $GITHUB_STEP_SUMMARY
          echo "1. 点击构建记录" >> $GITHUB_STEP_SUMMARY
          echo "2. 滚动到底部 Artifacts" >> $GITHUB_STEP_SUMMARY
          echo "3. 下载 app-release-apk" >> $GITHUB_STEP_SUMMARY
          echo "4. 解压得到 app-release.apk" >> $GITHUB_STEP_SUMMARY
