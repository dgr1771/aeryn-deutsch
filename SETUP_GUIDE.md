# 🚀 Aeryn-Deutsch 安装和使用完整指南

**写给计算机小白的简单教程**

---

## 📋 当前状态

### ✅ 我们已经完成了什么

1. ✅ **完整的设计**（就像房子的图纸）
2. ✅ **所有代码**（就像房子的框架）
3. ✅ **详细文档**（就像使用说明书）

### ⚠️ 现在需要做的

1. **安装工具**（安装Flutter，就像准备工具箱）
2. **配置环境**（设置工作台）
3. **运行项目**（开始工作）
4. **打包APK**（生成能安装的软件）

---

## 🔧 第一步：安装Flutter（30分钟）

### 什么是Flutter？
Flutter就是一个**工具包**，让你能把代码变成手机App。

### 怎么安装？

#### Windows用户：

1. **下载Flutter SDK**
   - 打开浏览器，访问：https://flutter.dev/docs/get-started/install/windows
   - 下载：`flutter_3.16.0-stable.zip`（约1GB）
   - 解压到：`C:\flutter`（一定要C盘根目录）

2. **配置环境变量**
   - 按 `Win + R`，输入：`sysdm.cpl`
   - 点击"环境变量"
   - 在"系统变量"里，新建：
     - 变量名：`Path`
     - 变量值：`C:\flutter\bin`
   - 点击"确定"

3. **验证安装**
   - 打开**新的**命令提示符（CMD）
   - 输入：
     ```bash
     flutter --version
     ```
   - 如果显示版本号（比如 3.16.0），说明安装成功！

#### Mac用户：

1. 打开终端，输入：
   ```bash
   brew install flutter
   ```

#### Linux用户（你现在的系统）：

```bash
# 1. 下载Flutter
cd ~
git clone https://github.com/flutter/flutter.git -b stable --depth 1

# 2. 添加到PATH
export PATH="$PATH:`pwd`/flutter/bin"

# 3. 验证
flutter --version
```

---

## 🎯 第二步：安装Android Studio（1小时）

### 什么是Android Studio？
这是**写Android App的软件**，就像文字处理软件用来写文档。

### 怎么安装？

1. **下载**
   - 访问：https://developer.android.com/studio
   - 下载Windows或Mac版本
   - 安装（一路点击"下一步"）

2. **安装Flutter插件**
   - 打开Android Studio
   - 点击 `Plugins`（插件）
   - 搜索 `Flutter`
   - 点击 `Install`（安装）
   - 重启Android Studio

---

## 📱 第三步：准备项目文件（5分钟）

### 把代码放到Windows能访问的地方

你现在的代码在Linux（WSL）里，但Windows的Android Studio访问不到。需要复制过去：

```bash
# 1. 在WSL里运行
cp -r /home/dgr/deyu/aeryn-deutsch /mnt/c/Users/你的用户名/aeryn-deutsch

# 例如，如果你的Windows用户名是67842
cp -r /home/dgr/deyu/aeryn-deutsch /mnt/c/Users/67842/aeryn-deutsch
```

---

## 🚀 第四步：打开项目（10分钟）

### 1. 启动Android Studio

### 2. 打开项目
- 点击 `File` → `Open`
- 选择：`C:\Users\你的用户名\aeryn-deutsch`
- 等待加载（第一次会很慢，需要下载Gradle，要10-20分钟）

### 3. 配置API Key
- 打开文件：`lib/main.dart`
- 找到第87行：
  ```dart
  static const String deepSeekApiKey = 'YOUR_DEEPSEEK_API_KEY_HERE';
  ```
- 替换成你的API Key

---

## ▶️ 第五步：运行App（5分钟）

### 在Android Studio里：

1. **选择设备**
   - 点击顶部的设备下拉框
   - 选择你的Android手机（或启动模拟器）

2. **运行**
   - 点击绿色的播放按钮 ▶️
   - 等待安装到手机

3. **完成！**
   - App会在你手机上运行

---

## 📦 第六步：打包APK（10分钟）

### 生成可以分享的安装包

在Android Studio里：

1. 点击 `Build` → `Flutter` → `Build APK`
2. 选择 `Release`
3. 等待生成
4. APK文件位置：`build/app/outputs/flutter-apk/app-release.apk`

---

## 🔧 常见问题解决

### 问题1：`flutter: command not found`

**原因**：Flutter没安装或没添加到PATH

**解决**：
```bash
# 临时解决（每次开新终端都要输入）
export PATH="$PATH:/home/你的用户名/flutter/bin"

# 永久解决（添加到 ~/.bashrc）
echo 'export PATH="$PATH:$HOME/flutter/bin"' >> ~/.bashrc
source ~/.bashrc
```

### 问题2：下载依赖很慢

**原因**：国内访问国外服务器慢

**解决**：使用国内镜像
```bash
export PUB_HOSTED_URL=https://pub.flutter-io.cn
export FLUTTER_STORAGE_BASE_URL=https://storage.flutter-io.cn
```

### 问题3：Gradle下载失败

**解决**：
- 打开文件：`aeryn-deutsch/android/build.gradle`
- 添加：
```gradle
repositories {
    maven { url 'https://maven.aliyun.com/repository/public' }
    maven { url 'https://maven.aliyun.com/repository/google' }
    maven { url 'https://maven.aliyun.com/repository/jcenter' }
    maven { url 'https://maven.aliyun.com/repository/central' }
}
```

---

## 📊 完成检查清单

在打包APK之前，确保：

- [ ] Flutter已安装（`flutter --version`有输出）
- [ ] Android Studio已安装
- [ ] 项目已打开
- [ ] API Key已配置
- [ ] 能在模拟器或真机上运行
- [ ] 所有功能测试通过

---

## 🎯 你的专属快速通道

### 最简单的路径（推荐）

**如果你想跳过安装，直接看效果**：

1. **联系我**（我帮你生成安装包）
2. **或者**：使用在线Flutter编辑器
   - 访问：https://dartpad.dev/
   - 可以运行简单的Dart代码

---

## 💡 计算机小白速记表

### 重要的命令

| 命令 | 作用 | 什么时候用 |
|------|------|----------|
| `ls` | 查看文件列表 | 想知道文件夹里有什么 |
| `cd 文件夹` | 进入文件夹 | 想去某个文件夹 |
| `pwd` | 查看当前位置 | 想知道自己在哪 |
| `cp 文件 目标` | 复制文件 | 想复制文件 |
| `nano 文件` | 编辑文件 | 想修改文件 |
| `Ctrl+O` | 保存 | nano编辑器保存 |
| `Ctrl+X` | 退出 | nano编辑器退出 |

### 文件位置说明

```
当前代码位置:
/home/dgr/deyu/aeryn-deutsch/

Windows访问路径:
\\wsl$\Ubuntu\home\dgr\deyu\aeryn-deutsch\

复制到Windows后:
C:\Users\你的用户名\aeryn-deutsch\
```

---

## 🆘 需要帮助？

### 如果遇到问题：

1. **截图发给我**
2. **告诉我错误信息**（复制红色文字）
3. **告诉我你在哪一步**

我会给你最简单的解决方案！

---

**最重要**：不用急，一步一步来！ 💪

（随时问我，我在这里帮你）
